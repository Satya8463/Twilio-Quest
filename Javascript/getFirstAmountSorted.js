function getFirstAmountSorted(inputArray, numberOfItems) {
     newArray=inputArray.sort();
    return newArray;
  }
  const outputArray = getFirstAmountSorted(['cat', 'apple', 'bat'], 2);
  console.log(newArray);